<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <style>
        body {
            background-color: #212121;
        }
    </style>
    <link rel="stylesheet" href="https://fonts.googleapis.com/icon?family=Material+Icons">
</head>

<link rel="stylesheet" href="{{ url_for('static', filename='style.css') }}">

<body>
    <div class="appbar">
        <a href="#" class="column">ติดต่อ</a>
        <a href="{{ url_for('home.homepage') }}" class="column">หน้าหลัก</a>
    </div>

    <div class="container">
        <form action="{{ url_for('login.login_route') }}" method="POST" class="form-box">
            <div class="form-link">
                <i class="material-icons" style='font-size:70px'>lock_open</i>
            </div>
            <div class="form-group form-inline">
                <input type="text" class="form-control" id="username" name="username" placeholder="ชื่อผู้ใช้งาน"
                    required>
            </div>
            <div class="form-group form-inline">
                <input type="password" class="form-control" id="password" name="password" placeholder="รหัสผ่าน"
                    required>
            </div>
            <div class="form-group button form-inline">
                <input type="submit" value="เข้าสู่ระบบ">
            </div>
            <div class="form-link">
                ยังไม่มีบัญชีใช่หรือไม่?
                <a href="{{ url_for('register.registerpage') }}"> ลงทะเบียน</a>


        </form>
    </div>
    <br>

</body>

</html>